---
const partnerImgs = await Astro.glob('./partnersImgs/*.png').then((imgs) => {
  return imgs.map((img) => img.default);
});

const clientsImgs = await Astro.glob('./clientsImgs/*.png').then((imgs) => {
  return imgs.map((img) => img.default);
});
---

<section id="clients" class="bg-white py-24 sm:pb-24 sm:pt-32">
  <!-- <div class="mx-auto mb-20 px-6 lg:w-[90vw] lg:px-8 xl:w-[70vw]">
    <h2
      class="mb-20 text-left text-5xl font-extrabold tracking-tight text-bravo md:mb-14 md:text-5xl lg:mb-16"
    >
      Meet our most trusted partners
    </h2>
    <div
      class="relative mx-auto flex flex-wrap justify-between gap-x-4 gap-y-6 p-8 before:absolute before:right-0 before:top-0 before:h-[10rem] before:w-[10rem] before:border-r-[3px] before:border-t-[3px] before:border-bravo before:bg-transparent after:absolute after:bottom-0 after:left-0 after:h-[10rem] after:w-[10rem] after:border-b-[3px] after:border-l-[3px] after:border-bravo after:bg-transparent md:justify-center md:gap-y-6 lg:w-[80vw] lg:gap-x-0 lg:p-2 xl:w-[60vw]"
    >
      {
        partnerImgs.map((img) => (
          <div class="flex h-28 flex-[0_1_40%] justify-center rounded-lg lg:flex-[0_0_30%]  ">
            <img
              class=" max-w-28 max-h-28 object-contain brightness-[0.8] grayscale "
              src={img}
              alt="Client Logo"
              width="158"
              height="48"
            />
          </div>
        ))
      }
    </div>
  </div> -->

  <div class="mx-auto px-6 lg:w-[90vw] lg:px-8 xl:w-[70vw]">
    <h2
      class="mb-20 text-left text-5xl font-extrabold tracking-tight text-bravo md:mb-14 md:text-5xl lg:mb-16"
    >
      Meet our most loved clients
    </h2>
    <div
      class="relative mx-auto flex flex-wrap justify-between gap-x-4 gap-y-6 p-8 before:absolute before:right-0 before:top-0 before:h-[10rem] before:w-[10rem] before:border-r-[3px] before:border-t-[3px] before:border-bravo before:bg-transparent after:absolute after:bottom-0 after:left-0 after:h-[10rem] after:w-[10rem] after:border-b-[3px] after:border-l-[3px] after:border-bravo after:bg-transparent md:justify-center md:gap-y-6 lg:w-[80vw] lg:gap-x-0 xl:w-[60vw]"
    >
      {
        clientsImgs.map((img) => (
          <div class="flex h-24 flex-[0_1_40%] justify-center rounded-lg lg:flex-[0_0_30%]  ">
            <img
              class=" max-w-20 max-h-20 object-contain brightness-50 grayscale"
              src={img}
              alt="Client Logo"
              width="158"
              height="48"
            />
          </div>
        ))
      }
    </div>
  </div>
</section>
